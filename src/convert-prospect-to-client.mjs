#!/usr/bin/env node

console.error("prospect conversion is retired: import a consented sprint application, record the human fit decision, then record paid Day 0; the service engine creates the canonical client scaffold automatically");
process.exit(1);
